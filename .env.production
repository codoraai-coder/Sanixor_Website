# Production build environment (committed — contains no secrets).
# Vite loads this automatically for `vite build`. The hosting dashboard's
# env var (if set) takes precedence over this file.
VITE_API_BASE_URL=https://api.sanixor.space
